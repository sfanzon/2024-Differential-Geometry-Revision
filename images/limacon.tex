\documentclass{standalone}
\usepackage{tikz}
\usetikzlibrary{angles,quotes}
\usetikzlibrary{decorations.markings}
\usepackage{pgfplots}

\begin{document}

\begin{tikzpicture}




 % Plot the curve in magenta with increased thickness
  \begin{axis}[
    domain=0:360, % Define the domain for t from 0 to 360 degrees (2*pi)
    samples=100, % Number of samples to draw the curve
    smooth, % Smoothly connect the points
    axis lines=center, % Centered axes
    axis line style={-latex}, % Arrows on axes
    xtick=\empty, % No x-axis ticks
    ytick=\empty, % No y-axis ticks
    scale=0.8, % Scale down the plot
    xmin=-2, xmax=4, % Extend x-axis by 1cm
    ymin=-3, ymax=3, % Extend y-axis by 1cm
  ]
    \addplot[magenta, thick] ((1+2*cos(x))*cos(x)), {(1+2*cos(x))*sin(x)});
  \end{axis}  
  
\end{tikzpicture}

\end{document}